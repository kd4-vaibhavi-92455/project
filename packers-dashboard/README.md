# company.js and companyCss.js: UI components.
- image setting(default value and setting URL.createOnbjectURL(e.target.files[0]))
- managed inputs via states(onChange=..).
- calling states and cities from db via backend queries and setting them on frontend in <option>